---
title: 第 17 课 · 数组、矩阵与广义表
createTime: 2026-09-05 18:42:07
permalink: /courses/zsb-cs/l/17/
banner: /images/covers/zsb-cs.png
---

> 阅读讲义后，可以[**打开互动练习**](/blog/lessons/zsb-cs/lessons/0017-array-matrix-glist.html)完成随堂测验、闪卡与复习。互动页顶部可随时返回课程目录和学习计划。

[课程大纲](/blog/lessons/zsb-cs/lessons/index.html) · 第二部分 数据结构
第 17 课 / 共 29 课

对应笔记 数据结构/13-数组、矩阵与广义表 · 教材 P80–88 · 约 15 分钟

::: tip 学习目标

学完这节课，你能做到：

① 手算一维/二维数组在行优先、列优先下的地址与元素序号；② 说出对称矩阵压缩所需单元数 n(n+1)/2 及稀疏矩阵三元组存储的代价；③ 正确求广义表的长度、深度（含空表深度 1）以及 GetHead/GetTail（表尾必须加括号）。

:::

::: details 温故 · 第 16 课「栈和队列」：循环队列的队满、队空条件是什么？元素个数怎么求？大小为 N 的数组最多能存几个元素？

队满 (rear+1)%MaxSize==front，队空 front==rear，元素个数 (rear-front+MaxSize)%MaxSize；因为要牺牲一个存储单元区分空满，大小为 N 的数组最多存 N-1 个元素。

:::

::: details 温故 · 第 15 课「线性表」：把结点 s 插到单链表中结点 p 之后，两句代码怎么写？顺序能反吗？

s-&gt;next=p-&gt;next; p-&gt;next=s;（口诀「先接后断」）。顺序不能反，若先执行 p-&gt;next=s，第二句 s-&gt;next 会指向 s 自己，造成断链。

:::

::: info

为什么这节课值得再过一遍

数组地址计算、矩阵压缩、广义表 Head/Tail 是数据结构部分填空与选择的高频组合题，往往一道题连环考两三个概念。巩固阶段的关键不是"看懂"，而是能在一分钟内不出错地推导。

:::

## 一、数组地址计算：先定顺序，再数元素

数组元素占用一段连续空间，类型相同、大小固定，支持下标随机访问。对数组只能做两种基本操作：**查找和修改**，不能插入删除。

### 1.1 一维数组

设首地址为 `LOC(a₀)`，每个元素占 `L` 字节：

```
<code>LOC(a[i]) = LOC(a[0]) + i × L</code>
```

### 1.2 二维数组的两种存储顺序

以 `int a[4][5]`（4 行 5 列）为例：

| 存储方式 | 地址公式 | a[2][3] 是第几个元素 |
| --- | --- | --- |
| 行优先 | `LOC = 基地址 + (i×列数 + j)×L` | 2×5+3+1 = **14** |
| 列优先 | `LOC = 基地址 + (j×行数 + i)×L` | 4×3+2+1 = **15** |

::: danger 易错

易错点

列优先不是 `j×列数`，而是 `j×行数`——因为一列里要"沾满"行数个元素。口诀：先看沾满几行/列，每行/列几个元素，再加上它本身。

:::

## 二、矩阵压缩：省空间，但失去随机存取

**压缩存储**：为多个值相同的元素只分配一个空间，对零元素不分配空间，目的是**减少存储空间**。

### 2.1 特殊矩阵

值相同或零元素分布有规律的矩阵：对称矩阵、上（下）三角矩阵、对角矩阵。

- **对称矩阵**：n 阶方阵满足 aᵢ,ⱼ = aⱼ,ᵢ，只需存下三角（含对角线），共需 **n(n+1)/2** 个存储单元。
- **三角矩阵**：下三角矩阵存下三角+对角线，上三角区为同一常量 c 时额外再存一个 c；上三角矩阵对称同理。

### 2.2 稀疏矩阵

- 非零元素个数 t 相对于总数 m×n 非常少；**稀疏因子 = t/(m×n)**。
- 用**三元组（行标 i、列标 j、值 v）**存储每个非零元素。
- ⚠️ **压缩存储后失去随机存取特性**——这是常考判断点。

::: info

辨析

数组是线性数据结构（连续存储、下标访问）；矩阵是数学概念（行列结构），计算机中用二维数组实现。所以"数组就是矩阵，矩阵就是数组"是**错误**的。

:::

## 三、广义表：长度、深度、表头、表尾

广义表是表元素可以是原子或广义表的线性表的推广。

- **长度** = 最外层元素个数。
- **深度** = 括号最大层数，**子表要展开**后数。
- **空表 A=()**：长度 0，**深度 1**。

| 广义表 | 长度 | 深度 | 说明 |
| --- | --- | --- | --- |
| A = () | 0 | 1 | 空表 |
| B = (d, e) | 2 | 1 | 元素全是原子 |
| C = (b, (c, d)) | 2 | 2 | 原子 b + 子表 |
| D = (B, C) | 2 | 3 | 展开后为 ((d,e),(b,(c,d))) |
| E = (a, E) | 2 | ∞ | 递归表 |

### 3.1 GetHead 与 GetTail

- **GetHead**：取第一个元素，可以是原子或子表。
- **GetTail**：去掉第一个元素后，**剩余元素加括号组成的表**——表尾一定是广义表。

例：D = ((a,(b,c)), (d,(e)))

- GetHead(D) = (a,(b,c))
- GetTail(D) = ((d,(e)))

::: danger 易错

高频错误

L=(a,(b,c)) 的表尾不是 (b,c)，而是 **((b,c))**；((a,b,c,d)) 的表尾是空表 **()**，因为外层只有一个元素，去掉后剩余为空。

:::

## 四、先回忆，再自测

::: details 合上笔记默写：行优先与列优先地址公式，并说明 a[2][3] 在 int a[4][5] 中分别是第几个元素

行优先：LOC = 基地址 + (i×列数 + j)×L，a[2][3] 是第 2×5+3+1=14 个；列优先：LOC = 基地址 + (j×行数 + i)×L，a[2][3] 是第 4×3+2+1=15 个。列优先不是 j×列数，而是 j×行数。

:::

::: details 默写：对称矩阵压缩单元数、稀疏矩阵存储结构与代价、空表深度

对称矩阵存下三角（含对角线），需 n(n+1)/2 个单元；稀疏矩阵用三元组 (i,j,v) 存储；压缩后失去随机存取特性；空表 () 的深度是 1，不是 0。

:::

::: details 判断正误并说明：GetHead 一定是广义表；GetTail 一定是广义表

第一句错误，第二句正确。GetHead 取第一个元素，可以是原子或子表；GetTail 去掉第一个元素后其余元素加括号组成，必定是广义表。

:::

## 五、测验（点击选项作答）

**1. 对于 `int a[4][5]`，按行优先存储时 `a[2][3]` 是第几个元素**

- A. 第 13 个元素
- B. 第 14 个元素
- C. 第 15 个元素
- D. 第 16 个元素

::: details 点开核对答案

**答案：B（第 14 个元素）** —— 行优先公式：i×列数+j+1 = 2×5+3+1 = 14。列优先才是 4×3+2+1 = 15。

:::

**2. n 阶对称矩阵采用压缩存储，至少需要多少个存储单元**

- A. n² 个存储单元
- B. n(n−1)/2 个单元
- C. n(n+1)/2 个单元
- D. (n+1)²/2 个单元

::: details 点开核对答案

**答案：C（n(n+1)/2 个单元）** —— 只存下三角（含对角线）：1+2+…+n = n(n+1)/2。

:::

**3. 稀疏矩阵用三元组存储后，下列说法正确的是**

- A. 仍可直接随机存取
- B. 失去随机存取特性
- C. 访问速度明显更快
- D. 必须按行号顺序存取

::: details 点开核对答案

**答案：B（失去随机存取特性）** —— 三元组只记录非零元素的行、列、值，无法通过下标直接定位元素，因此失去随机存取特性。

:::

**4. 已知 B=(d,e)，C=(b,(c,d))，则 D=(B,C) 的深度为**

- A. 1 层括号深度
- B. 2 层括号深度
- C. 3 层括号深度
- D. 4 层括号深度

::: details 点开核对答案

**答案：C（3 层括号深度）** —— 求深度要把子表展开：D=((d,e),(b,(c,d)))，最内层 (c,d) 有 3 层括号，故深度为 3。

:::

**5. 广义表 L=(a,(b,c))，GetTail(L) 的结果是**

- A. (b,c) 一个子表
- B. ((b,c)) 一个表
- C. b,c 两个原子
- D. c 单个原子

::: details 点开核对答案

**答案：B（((b,c)) 一个表）** —— 表尾是去掉第一个元素后其余元素加括号组成的表，因此是 ((b,c))，不是 (b,c)。

:::

**6. 关于空广义表 A=()，下列说法正确的是**

- A. 长度为 0，深度为 0
- B. 长度为 0，深度为 1
- C. 长度为 1，深度为 0
- D. 长度为 1，深度为 1

::: details 点开核对答案

**答案：B（长度为 0，深度为 1）** —— 空表最外层元素个数为 0，故长度为 0；但它本身是一对括号构成的表，深度为 1。

:::

::: warning 动手环节

课后任务（约 20 分钟）

打开 数据结构/13-数组、矩阵与广义表，完成笔记内 24 道练习：随堂练习 12 题（编号 1–10、14–15）、判断题 6 道、自测问题 Q1–Q6（Q4–Q6 稀疏矩阵三元组、长度深度、Head/Tail 正是本课测验重点）。错题用 ⭐️ 标出，重点回看「易错点与对比」表，然后再遮住答案做一次。

:::

## 参考资料

首选精读：数据结构/13-数组、矩阵与广义表（含 12 道随堂练习、6 道判断、自测问答与易错点表）。术语速查：[课程术语表](/blog/lessons/zsb-cs/reference/glossary.html)、[数据结构公式速查](/blog/lessons/zsb-cs/reference/ds-formula-cheatsheet.html)。教材对照：P80–P88。

::: info 老师的话

有哪里卡住了？直接把题目或疑问发给我——我是你的课程老师，可以逐条讲清楚，也可以针对你错的题再出几道同类题。

:::

[← 上一课：栈和队列](/courses/zsb-cs/l/16/)
[下一课：串 →](/courses/zsb-cs/l/18/)

---

[← 第 16 课](/courses/zsb-cs/l/16/) · [课程目录](/courses/zsb-cs/) · [第 18 课 →](/courses/zsb-cs/l/18/)

