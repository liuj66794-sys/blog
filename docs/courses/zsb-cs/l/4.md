---
title: 第 4 课 · 顺序结构：printf 与 scanf
createTime: 2026-09-05 18:31:20
permalink: /courses/zsb-cs/l/4/
banner: /images/covers/zsb-cs.png
---

> 阅读讲义后，可以[**打开互动练习**](/blog/lessons/zsb-cs/lessons/0004-sequential-io.html)完成随堂测验、闪卡与复习。互动页顶部可随时返回课程目录和学习计划。

[课程大纲](/blog/lessons/zsb-cs/lessons/index.html) · 第一部分 C 语言
第 4 课 / 共 29 课

对应笔记 C语言/03-顺序结构程序设计 · 教材 P59–83 · 约 15 分钟

::: tip 学习目标

学完这节课，你能做到：

① 给定 printf 格式串和输出项，能写出屏幕上的实际结果（含 %m.nf 列宽、四舍五入与对齐）；② 看到 scanf 格式串，能判断用户应该怎么输入（普通字符、%c、%lf 等陷阱）；③ 正确使用 putchar / getchar 处理单字符输入输出。

:::

::: details 温故 · 第 3 课「运算符与表达式」：a=10 时，表达式 a += a -= a*a 的结果是多少？

赋值右结合：先算 a -= a*a，得 a = 10 − 100 = −90；再算 a += (−90)，得 a = −180。

:::

::: details 温故 · 第 2 课「数据的表示：数制、补码与常量」：十进制转成非十进制，整数和小数部分分别怎么做？175 的二进制、八进制、十六进制各是什么？

整数部分「除 N 取余，倒序输出」；小数部分「乘 N 取整，正序输出」。175 = 10101111B = 257Q = AFH。

:::

::: info

为什么这节课关键

printf / scanf 是广东专升本程序阅读题和选择题的最大题源之一，尤其是 `%m.nf` 输出、`double` 必须用 `%lf`、`%c` 会吃空格、格式串中普通字符要原样输入——这四点每年必考。

:::

## 一、printf 格式输出

printf 的格式是 `printf(格式控制字符串, 输出列表);`。格式控制串里分为两类内容：

- **普通字符**：原样输出，例如 `printf("a=%d", 100);` 中的 `a=`。
- **格式声明符**：`%` 开头，对应输出列表中的一项；有几个格式符就应有几项，**缺项会输出随机值，多余项不输出**。

| 格式符 | 含义 | 高频示例 |
| --- | --- | --- |
| `%d` | 有符号十进制整数 | `printf("%d", 100);` → 100 |
| `%md` / `%-md` | 指定列宽；正数右对齐、负数左对齐 | `%5d` 输出 100 → `□□100` |
| `%f` | 实型，默认 6 位小数 | `printf("%f", 1/2.0);` → 0.500000 |
| `%m.nf` | 宽度 m，保留 n 位小数（四舍五入） | `%6.3f` 输出 -3.1415 → `-3.142`（"-3.142" 本身 6 字符，宽度 6 恰好装满，不补空格；`%7.3f` 才得 `□-3.142`） |
| `%.nf` | 只保留 n 位小数 | `%.2f` 输出 3.1415 → 3.14 |
| `%c` | 单个字符（不带单引号） | `printf("%c", 97);` → a |
| `%s` | 字符串（不带双引号） | `printf("%s", "hi");` → hi |
| `%o` / `%x` | 八进制 / 十六进制，不带前缀、不带符号 | `%#o` / `%#x` 才带 0 / 0x 前缀 |
| `%e` | 指数形式，默认 6 位小数 | 123.456 → 1.234560e+02 |
| `%%` | 输出一个 % | `printf("%%");` → %；陷阱：`printf("a=%%d,b=%%d",a,c);` 输出 `a=%d,b=%d`，参数 a、c 不输出 |
| `%u` | 无符号十进制 | 2 字节 unsigned a = -1 → 65535 |

::: danger 易错

⚠️ 列宽与四舍五入规则

`%m.nf` 的 m 是**总宽度**，小数点、负号都算进宽度；n 位小数按四舍五入。规则：**m 为正左补空格，m 为负右补空格，数据实际宽度 ≥ |m| 时原样输出**。

例如 `printf("%8.2f", -100.125);`：先四舍五入为 -100.13，总宽度 8，当前占 7 位，所以左补一个空格 → `□-100.13`；而 `%-9.2f` 则左对齐 → `-100.13□□`。

:::

**C 语句分类速记**：五种语句——表达式语句、函数调用语句、控制语句、复合语句、空语句。复合语句整体算一条语句，最后一个 `}` 后**不加分号**；`#include`、`#define` 是预处理指令，不是语句。

## 二、scanf 格式输入

scanf 的格式是 `scanf(格式控制字符串, 地址列表);`，地址列表必须是 `&变量名`。scanf 是给变量赋值，必须知道变量地址，没有 `&` 数据存不进去。

| 格式串写法 | 正确输入方式 | 常见错误 |
| --- | --- | --- |
| `%d%d` | 用空格、回车或 Tab 分隔，如 `10 20` | 用逗号分隔 |
| `%d,%d` | 逗号必须原样输入，如 `10,20` | 输入 `10 20` |
| `a=%d` | 普通字符原样输入，如 `a=2` | 只输 2 |
| `%c%c%c` | 连续输入 3 个字符，如 `abc` | 输入 `a b c`，空格会被当字符读入 |
| `%c,%c,%c` | `q,w,e`，逗号也要输入 | 用空格替代逗号 |
| `%3d` | 输入 12345 只取 123，剩余留给下一个变量 | 以为会读完整数 |

::: danger 易错

⚠️ scanf 两大硬规则

- **double 必须 %lf：** `double x; scanf("%f", &x);` 无法正确输入；必须用 `%lf`。printf 输出时 float / double 都可以用 %f。
- **C 没有 %m.nf 形式的输入：** 形如 `scanf("%5.2f", &x);` 的写法直接判错。
- **%c 会吃空格 / 回车：** `scanf("%c%d", &ch, &a);` 输入 `123`，ch 得到 `'1'`，a 得到 `23`。

:::

## 三、putchar 与 getchar

- `int putchar(int ch);`：输出**单个字符**，参数可以是字符常量、字符变量、ASCII 码或转义符。例如 `putchar('A');` → A，`putchar(97);` → a。
- `int getchar(void);`：从键盘缓冲区读一个字符；按回车后才从 stdin 逐个读取，**多余字符留在缓冲区**，等待后续 getchar 读取。

::: info

小应用：大写转小写

大写字母 ASCII +32 即得小写。教材例题：`c2 = c1 + 32;` 或直接把 `getchar() + 32` 传给 `putchar()` / `printf("%c", ...)`。

:::

## 四、先回忆，再自测

::: details 合上笔记回答：printf("%8.2f", -100.125); 输出什么？□ 代表空格。

先四舍五入为 -100.13，总宽度 8，当前 7 位，左补一个空格 → `□-100.13`。

:::

::: details 合上笔记回答：char ch; int a; scanf("%c%d", &ch, &a); 输入 123 后，ch 和 a 分别是？

ch = '1'，a = 23。%c 只读一个字符，剩下的 "23" 按 %d 解析。

:::

::: details 合上笔记回答：double x; 用 scanf 输入时格式符应该写什么？

必须用 `%lf`：`scanf("%lf", &x);`。%f 无法正确读入 double。

:::

::: details 合上笔记回答：C 语句分哪五种？复合语句末尾要加分号吗？

表达式语句、函数调用语句、控制语句、复合语句、空语句。复合语句整体算一条语句，最后的 `}` 后不加分号；#define 等预处理指令不是语句。

:::

## 五、测验（点击选项作答）

**1. 执行 `printf("%6.2f", -3.1415);`，输出结果是**

- A. 输出 `-3.14`
- B. 输出 `□-3.14`
- C. 输出 `-3.142`
- D. 输出 `□-3.142`

::: details 点开核对答案

**答案：B（输出 □-3.14）** —— %6.2f 保留 2 位小数，-3.1415 四舍五入为 -3.14；总宽度 6，当前 5 位，左补一个空格 → □-3.14。

:::

**2. 关于 scanf 的输入规则，正确的是**

- A. double 变量可用 %f 正确输入
- B. 格式串中的逗号必须原样输入
- C. %c 会自动跳过空格再读取字符
- D. scanf 支持 %5.2f 这种输入格式

::: details 点开核对答案

**答案：B（格式串中的逗号必须原样输入）** —— double 必须 %lf；%c 会接收空格；C 不提供 %m.nf 输入。普通字符（如逗号）必须按格式串原样输入。

:::

**3. 执行 `char c; scanf("%c", &c);`，输入 `5`（先空格后 5）后，c 的值是**

- A. 字符 `'5'`
- B. 字符 `' '`
- C. 整数 5
- D. 随机字符

::: details 点开核对答案

**答案：B（字符 ' '）** —— %c 读取单个字符，不会跳过空格，第一个输入的字符就是空格，所以 c = ' '。

:::

**4. 执行 `putchar(97);`，屏幕输出是**

- A. 输出字符 a
- B. 输出字符 9
- C. 输出整数 97
- D. 输出字符串 "97"

::: details 点开核对答案

**答案：A（输出字符 a）** —— putchar 输出单个字符；97 是 'a' 的 ASCII 码，所以输出 a。

:::

**5. 执行 `printf("%-6.2f", 3.1415);`，输出结果是**

- A. 输出 `□□3.14`
- B. 输出 `3.14□□`
- C. 输出 `3.1415`
- D. 输出 `3.142□`

::: details 点开核对答案

**答案：B（输出 3.14□□）** —— 负号表示左对齐；保留 2 位小数后 3.14 占 4 位，宽度 6，右侧补两个空格 → 3.14□□。

:::

**6. 执行 `double x; scanf("%lf", &x);`，输入 `1.5` 后，x 的值是**

- A. 1
- B. 1.5
- C. 1.05
- D. 不确定

::: details 点开核对答案

**答案：B（1.5）** —— double 用 %lf 输入能正确接收，x 的值就是 1.5；1.05 无从得出——%.1f、%.2f 这类格式符只在输出显示时四舍五入，不改变变量的存储值。

:::

::: warning 动手环节

课后任务（约 20 分钟）

打开 题库/顺序程序设计-章节练习，遮住答案完整做一遍，错题标 ⭐️。然后回到笔记 自测问题 Q1–Q10，遮住答案口头作答，卡壳的记入错题本。

:::

## 参考资料

首选精读：C语言/03-顺序结构程序设计（含「易错点与对比」表和自测问题）。格式符速查：[C 语法速查表](/blog/lessons/zsb-cs/reference/c-syntax-cheatsheet.html)。术语速查：[课程术语表](/blog/lessons/zsb-cs/reference/glossary.html)。

::: info 老师的话

有哪里卡住了？直接把题目或疑问发给我——我是你的课程老师，可以逐条讲清楚，也可以针对你错的题再出几道同类题。

:::

[← 上一课：运算符与表达式](/courses/zsb-cs/l/3/)
[下一课：选择结构基础 →](/courses/zsb-cs/l/5/)

---

[← 第 3 课](/courses/zsb-cs/l/3/) · [课程目录](/courses/zsb-cs/) · [第 5 课 →](/courses/zsb-cs/l/5/)

