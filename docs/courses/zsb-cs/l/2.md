---
title: 第 2 课 · 数据的表示：数制、补码与常量
createTime: 2026-09-05 18:30:47
permalink: /courses/zsb-cs/l/2/
banner: /images/covers/zsb-cs.png
---

> 这是本课阅读版。[**打开互动课程**](/blog/lessons/zsb-cs/lessons/0002-data-representation.html)即可在同一页阅读、作答与复习，学习记录保存在此设备。

[课程大纲](/blog/lessons/zsb-cs/lessons/index.html) · 第一部分 C 语言
第 2 课 / 共 29 课

对应笔记 C语言/02-数据存储与运算 · 教材 P41–P47 · 约 15 分钟

::: tip 学习目标

学完这节课，你能做到：

① 在二进制、八进制、十进制、十六进制之间互转并写出过程；② 一眼判断整型/实型常量、用户标识符是否合法；③ 正数与负数的原码、反码、补码换算；④ 解释 char 溢出后的回绕现象。

:::

::: details 温故 · 第 1 课「C 程序的全貌与编译四步骤」：C 程序从源代码到运行要经过哪四步？每步的产物是什么？

编辑→.c（文本）；编译→.obj（二进制）；连接→.exe（二进制）；运行→结果。

:::

::: details 温故 · 第 1 课「C 程序的全貌与编译四步骤」：「程序 = ？+ ？」算法的三种基本结构是什么？

程序 = 算法 + 数据结构；三种基本结构：顺序、选择、循环。

:::

::: info

为什么这节课重要

数制转换、补码、常量合法性、标识符合法性几乎是每次必考，也是理解后面运算符、表达式、数组指针的地基。你已经学过一轮——这节课把源笔记前半部分压缩成可快速检索的回忆链。

:::

## 一、存储单位：bit 与 Byte

计算机内部，**数据以二进制形式存储和运算**。最小单位是 **bit（位）**，数据处理和存储容量的基本单位是 **Byte（字节）**。

| 换算 | 说明 |
| --- | --- |
| 1 Byte = 8 bit | bit 是最小单位 |
| 1 KB = 1024 B = 2¹⁰ B | 千字节 |
| 1 MB = 1024 KB = 2²⁰ B | 兆字节 |
| 1 GB = 1024 MB = 2³⁰ B | 吉字节 |
| 1 TB = 1024 GB = 2⁴⁰ B | 太字节 |

## 二、数制与转换

四个概念：数码、基数、位权、尾符。

| 进制 | 数码 | 基数 | 尾符 |
| --- | --- | --- | --- |
| 二进制 | 0~1 | 2 | B |
| 八进制 | 0~7 | 8 | O/Q |
| 十进制 | 0~9 | 10 | D |
| 十六进制 | 0~9、A~F | 16 | H |

转换口诀：

- 非十进制 → 十进制：**乘权求和**。例：1101B = 1×2³+1×2²+0×2¹+1×2⁰ = 13D。
- 十进制 → 非十进制：整数 **除 N 取余，倒序输出**；小数 **乘 N 取整，正序输出**。
- 二进制 ↔ 八进制：**每 3 位二进制 = 1 位八进制**。
- 二进制 ↔ 十六进制：**每 4 位二进制 = 1 位十六进制**。

::: info

简答常考

175D = 10101111B = 257Q = AFH。要能写出除基取余的过程。

:::

## 三、数据类型与原反补码

基本类型中先死记这四个：

| 类型 | 字节 | 位数 | 范围 |
| --- | --- | --- | --- |
| 有符号 char（本课按 8 位二补码示例） | 1 | 8 | -128 ~ 127（-2⁷ ~ 2⁷-1） |
| unsigned char | 1 | 8 | 0 ~ 255（0 ~ 2⁸-1） |
| short | 2 | 16 | -2¹⁵ ~ 2¹⁵-1 |
| int / long | 4 | 32 | -2³¹ ~ 2³¹-1 |
| float | 4 | 32 | 约 6 位有效数字 |
| double | 8 | 64 | 约 15 位有效数字 |

::: danger 易错

易错点

实型常量如 `3.14` 默认按 **double** 处理，不是 float；写 `3.14f` 或 `3.14F` 才是 float。

:::

### 原码、反码、补码

- 最高位是符号位：**0 正 1 负**。
- 无符号整数：二进制原样存储；有符号整数：**补码**存储；字符：**ASCII**存储。
- 正数：原码 = 反码 = 补码。
- 负数：反码 = 符号位不变，数值位取反；补码 = 反码 + 1。

例：-11（8 位）

| 码 | 值 |
| --- | --- |
| 原码 | 10001011 |
| 反码 | 11110100 |
| 补码 | 11110101 |

::: danger 易错

特殊值

8 位有符号数中，**-128 只有补码 10000000**，没有原码和反码。

:::

### char 溢出回绕

以下按本课示例约定：char 为 8 位有符号二补码，超出可表示范围时按 ±256 回绕；实际 C 实现的 char 位宽与 signedness 可能不同。

- `char c = 128;` → 输出 **-128**（128 - 256 = -128）
- `char c = -129;` → 输出 **127**（-129 + 256 = 127）
- `unsigned char c = -1;` → 输出 **255**（补码 11111111 按无符号读 = 2⁸-1）

## 四、常量与标识符

### 整型常量

| 进制 | 特征 | 合法例 | 非法例 |
| --- | --- | --- | --- |
| 十进制 | 普通数字 | 100、-12 | — |
| 八进制 | 0 开头，数码 0~7 | 066、023L | 66（缺 0）、08C2（含 8/C） |
| 十六进制 | 0x/0X 开头 | 0x66、0xACF | A8（缺前缀）、0x8CG（含 G） |

### 实型常量

- 十进制形式：3.14、.2、-9.，口诀是**「点的一侧要有数」**。
- 指数形式 aEn：口诀是**「e 前 e 后要有数，e 后必须是整数」**。如 `3.14e2`、`.7e-2` 合法；`7e0.0`、`6e` 不合法。

### 字符与字符串

| 对比 | 'a' | "a" |
| --- | --- | --- |
| 引号 | 单引号 | 双引号 |
| 内存 | 1 字节 | 2 字节（含结束符 '\0'） |
| 含义 | 整型值（ASCII 码） | 地址值 |

常用转义字符速记：`\n` 换行、`\t` 制表、`\\` 反斜杠、`\'` 与 `\"` 引号；`\ddd` 八进制、`\xhh` 十六进制。一个转义字符只占 1 字节，如 `'\101'` 就是 'A'。

::: danger 易错

⚠️ 易错区分

`'\101'` 是字符型，内存占 1 字节；`0101` 是整型八进制常量，占 4 字节。两者不要混淆。

:::

### 标识符

用户自定义标识符规则：

1. 只能由字母、数字、下划线组成。
2. 数字不能开头。
3. 不能用关键字。
4. 区分大小写（`Int` 合法，`int` 不合法）。

::: danger 易错

高频陷阱

`9_hool`（数字开头）、`he.llo`（含点）、`x￥yz`（含￥）、`int`（关键字）、`Text-0`（含减号）均不合法。

:::

### 符号常量与 const 常变量

`#define PI 3.14`：编译前把 PI **文本替换**为 3.14，**不占内存**；它是预处理指令不是语句，**末尾不加分号**。`const` 常变量则**占内存**、有类型。

::: danger 易错

宏定义陷阱

`#define N 5` 后写 `N++` 不合法——N 是符号常量，不是变量。

:::

## 五、先回忆，再自测

::: details 合上笔记默写：十进制转非十进制的口诀、175 的三种进制表示

整数部分「除 N 取余，倒序输出」；小数部分「乘 N 取整，正序输出」。175 = 10101111B = 257Q = AFH。

:::

::: details 负数的反码、补码怎么求？-11 的 8 位原码、反码、补码各是什么？

反码：符号位不变，数值位取反；补码：反码 + 1。-11：原码 10001011、反码 11110100、补码 11110101。

:::

::: details 用户标识符的三条硬规则是什么？判断 _buy_2、2_buy、Int、char 的合法性

字母/数字/下划线组成、数字不开头、不用关键字（区分大小写）。_buy_2 ✓、2_buy ✗（数字开头）、Int ✓、char ✗（关键字）。

:::

## 六、测验（点击选项作答）

**1. 十进制 175 对应的二进制、八进制、十六进制是**

- A. 10101111B、257Q、AFH
- B. 10101111B、267Q、AFH
- C. 10111111B、257Q、AFH
- D. 10101111B、257Q、AEFH

::: details 点开核对答案

**答案：A（10101111B、257Q、AFH）** —— 175 除 2 取余得 10101111B；三位一组得 257Q；四位一组得 AFH。

:::

**2. 下列整型常量中，合法的是**

- A. 08C2
- B. 0x8CG
- C. 066
- D. A8

::: details 点开核对答案

**答案：C（066）** —— 八进制以 0 开头且只能含 0~7；十六进制以 0x 开头且只能含 0~9/A~F；A8 缺少 0x 前缀。

:::

**3. 下列实型常量中，不合法的是**

- A. .7e-2
- B. 3.14e2
- C. 7e0.0
- D. -9.

::: details 点开核对答案

**答案：C（7e0.0）** —— 指数形式要求 e 后必须是整数，7e0.0 违反此规则；.7e-2、3.14e2、-9. 均合法。

:::

**4. 8 位有符号数 -11 的补码是**

- A. 10001011
- B. 11110100
- C. 11110101
- D. 11110110

::: details 点开核对答案

**答案：C（11110101）** —— -11 原码 10001011，数值位取反得反码 11110100，加 1 得补码 11110101。

:::

**5. 在本课约定的 8 位有符号 char 且按模 256 回绕的环境下，执行 char c = 128; printf("%d", c); 的输出是**

- A. 128
- B. 0
- C. -128
- D. 127

::: details 点开核对答案

**答案：C（-128）** —— 在上述约定下，char 上限为 127，128 按 8 位有符号二补码回绕为 -128。

:::

**6. 下列用户标识符中，合法的是**

- A. 2_buy
- B. int
- C. _buy_2
- D. Text-0

::: details 点开核对答案

**答案：C（_buy_2）** —— 2_buy 数字开头，int 是关键字，Text-0 含减号，均不合法；_buy_2 符合字母/数字/下划线且非关键字。

:::

**7. 定义 char c = 'a'; char s[] = "a"; 后，sizeof(c) 与 sizeof(s) 分别是**

- A. c 占 1 字节，s 占 2 字节
- B. c 占 2 字节，s 占 1 字节
- C. 都占 1 字节
- D. 都占 2 字节

::: details 点开核对答案

**答案：A（c 占 1 字节，s 占 2 字节）** —— sizeof(char) 按定义为 1 个 C 字节，所以 c 占 1 字节；s 是包含 a 和末尾空字符的 char 数组，占 2 字节。注意：标准 C 中字符常量本身的类型是 int，sizeof('a') 等于 sizeof(int)，不能与 sizeof(c) 混淆。

:::

::: warning 动手环节

课后任务（约 20 分钟）

打开 题库/数据存储与运算-章节练习，完成前半部分（数制转换、数据类型、常量与标识符相关题目），错题标 ⭐️。然后回到笔记 自测问题 Q1–Q9，遮住答案口头作答，卡壳的记入错题本。

:::

## 参考资料

首选精读：C语言/02-数据存储与运算（前半：存储单位、数制、数据类型、原反补码、常量与标识符）。术语速查：[课程术语表](/blog/lessons/zsb-cs/reference/glossary.html)、[C 语法速查表](/blog/lessons/zsb-cs/reference/c-syntax-cheatsheet.html)。

::: info 老师的话

有哪里卡住了？直接把题目或疑问发给我——我是你的课程老师，可以逐条讲清楚，也可以针对你错的题再出几道同类题。

:::

[← 上一课：C 程序的全貌与编译四步骤](/courses/zsb-cs/l/1/)
[下一课：运算符与表达式 →](/courses/zsb-cs/l/3/)

---

[← 第 1 课](/courses/zsb-cs/l/1/) · [课程目录](/courses/zsb-cs/) · [第 3 课 →](/courses/zsb-cs/l/3/)

