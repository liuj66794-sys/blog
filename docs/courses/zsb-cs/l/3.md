---
title: 第 3 课 · 运算符与表达式
createTime: 2026-09-04 19:22:57
permalink: /courses/zsb-cs/l/3/
banner: /images/covers/zsb-cs.png
---

> 阅读讲义后，可以[**打开互动练习**](/blog/lessons/zsb-cs/lessons/0003-operators-expressions.html)完成随堂测验、闪卡与复习。互动页顶部可随时返回课程目录和学习计划。

[课程大纲](/blog/lessons/zsb-cs/lessons/index.html) · 第一部分 C 语言程序设计
第 3 课 / 共 29 课

对应笔记 C语言/02-数据存储与运算 §7 · 教材 P55–64 · 约 15 分钟

::: tip 学习目标

学完这节课，你能做到：

① 背出优先级关键链「括号 &gt; 单目 &gt; 算术 &gt; 移位 &gt; 关系 &gt; 位与/异或/或 &gt; 逻辑 &gt; 条件 &gt; 赋值 &gt; 逗号」，并说出三种**右结合**的运算符；② 口算 `7/3`、`7%3`、`-5%3`、`7/2.0`；③ 辨析 `++i` 与 `i++`，判断 `6++`、`(a+1)++` 不合法；④ 一步一步推出必考题 `a += a -= a*a`；⑤ 写出逗号表达式、赋值表达式的取值规则，并完成代数式 → C 表达式的转写。

:::

::: details 温故 · 第 2 课「数据的表示：数制、补码与常量」：负数的反码、补码怎么求？-11 的 8 位原码、反码、补码各是什么？

反码：符号位不变，数值位取反；补码：反码 + 1。-11：原码 10001011、反码 11110100、补码 11110101。

:::

::: details 温故 · 第 1 课「C 程序的全貌与编译四步骤」：.c 源程序、.obj 目标程序、.exe 文件中，哪一种能被计算机直接执行？

源程序（.c）和目标程序（.obj）都不能被计算机直接执行，只有 .exe 能；能被计算机直接执行的语言也只有机器语言。

:::

::: info

为什么这一课单独成课

上一课的数制与补码是「数据怎么存」，这一课是「数据怎么算」——第二章的 **§7 是全章最重的一节（★★★★★）**。2025 真题卷第 2、13、18 题全部出自这里：表达式求值、`a+=a-=a*a`、优先级最高的运算符。

:::

## 一、优先级：先记骨架，再记右结合

15 级优先级不必逐级死背，先背这条**骨架链**（高 → 低）：

括号/下标 &gt; 单目 &gt; 算术 &gt; 移位 &gt; 关系 &gt; 位（与&gt;异或&gt;或）&gt; 逻辑 &gt; 条件 &gt; 赋值 &gt; 逗号

两条使用规则：

- **不同优先级**看级别——级别高的先算；
- **相同优先级**看结合方向——绝大多数从左至右（如 `5+6-2 = (5+6)-2`）。

::: danger 易错

只有三种结合方向是「从右至左」

① 单目运算符（`! ~ ++ -- - (类型) * & sizeof`，2 级）；② 条件 `?:`（13 级）；③ 赋值与复合赋值（14 级）。选择题问「右结合的运算符」就从这三档里找。2025 真题第 18 题：`~` 是 2 级单目，优先级高于 `/`、`<<`、`&&`。

:::

## 二、算术运算符：/ 整除、% 取余、++ 自增

### 2.1 除法 `/` 与取余 `%`

| 规则 | 例 |
| --- | --- |
| 两侧**均为整数** → 只保留整数部分 | `1/3=0`、`7/3=2`、`1/2=0` |
| 有一侧是**小数** → 结果为 double | `7/2.0=3.5`、`1.0/2=0.5` |
| `%` 的操作数**必须是整型**【经典选择】 | `4*2.5%3` 不合法（2.5 是实数） |
| `%` 的**余数符号与被除数一致** | `-5%3=-2`、`5%-2=2`、`-5%-2=-2` |

经典易错：`3.5+1/2` = 3.5 + 0 = **3.5**，不是 4.0——`1/2` 先按整除算成 0。

### 2.2 自增自减 `++` `--`

- 只能用于**变量**：`6++`、`(a+1)++`、`(a++)++` 全部**不合法**【常考易错】；
- **前置 `++i`**：先加 1 再使用；**后置 `i++`**：「后置看整体，之后变量必改值」——先把 `i++` 整体当当前值用，语句之后 i 变为 i+1。

例：i=10 时，`int a = i++;` → a=10，i=11；`int a = ++i;` → a=11，i=11。

c 语言例：a=3, b=9 时 `c = b/++a` → a 先变 4，c = 9/4 = **2**。

## 三、类型转换：混合运算、强制转换与赋值转换

- **混合运算**：操作数中有 float/double → 结果为 double；`30+2.5*3-1/2` = 30+7.5-0 = **37.5**；`'A'+3` = 68（char 按 ASCII 参与运算，68 是 `'D'`）。
- **强制转换** `(类型)(表达式)`：优先级 2（单目），`(int)x+y` 只转 x。**不改变变量原值**：x=3.14 时 `(int)x` 这个整体的值是 3，x 仍是 3.14。真题：`((double)4/2)+2.5` = 2.0+2.5 = **4.5**。
- **赋值自动转换**：实→整**舍去小数**（`int a=3.14` → a=3）；整→实补 0；char→int 放 ASCII 值。链式赋值：`d = b = c = a = 3.14;`（a、b 为 int）→ a=3, b=3, c=3.0, d=3.0。

## 四、赋值与复合赋值【必考】

- 赋值号**左侧不可以是算式**：`a+b=2`、`(a=b)=c`、`(a++)=8` 均不合法；
- 赋值表达式**本身也有值**：`x=(a=1)+(b=3)` → x=4；
- 复合赋值 `+= -= *= /= %=` 等：`x *= a+b` ≡ `x = x*(a+b)`——**右边整体要加括号**。

::: danger 易错

必考题：a=10，求 a += a -= a*a

右结合 → 先算 `a -= a*a`：a = 10 − 100 = **−90**；再算 `a += (−90)`：a = −90 + (−90) = **−180**。

同型题 n=2：`n += n -= n*n` → 先 2−4=−2，再 −2+(−2) = **−4**。2025 真题第 13 题同款：a=6 时结果为 −60。

:::

## 五、逗号表达式

- 从左到右逐个计算，**整体值 = 最后一个表达式的值**：`a=(10,20,30)` → a=30；
- 优先级**最低**（15 级）——`y = a=1, a+5, b=a+10` 里赋值优先级高于逗号，先算 `y = a=1`；
- 中途变量被改变要跟住：double x 时 `x++, x+10, x=0` 的值为 0.0。

代数式转写两条硬规矩：乘号不能省（3a 写 `3*a`）；除法整体要括号（a÷(3+2) 写 `a/(3+2)`）。

## 六、先回忆，再自测

::: details 优先级骨架链是什么？哪三档是右结合？

括号/下标 &gt; 单目 &gt; 算术 &gt; 移位 &gt; 关系 &gt; 位（与&gt;异或&gt;或）&gt; 逻辑 &gt; 条件 &gt; 赋值 &gt; 逗号。右结合：单目、`?:`、赋值（含复合赋值）。

:::

::: details 7/3、7%3、-5%3、5%-2、7/2.0 各是多少？

2；1；−2（余数符号同被除数）；2；3.5（有实数参与结果为 double）。

:::

::: details a=10，a += a -= a*a 的完整推导？

右结合：先 `a -= a*a` 得 a=10−100=−90；再 `a += (−90)` 得 a=−180。口诀：先把右边吃掉自己，再吃一遍。

:::

::: details (a+1)++ 合法吗？++i 与 i++ 的口诀？

不合法——自增只能用于变量。前置先加后用；后置「后置看整体，之后变量必改值」。

:::

## 七、测验（点击选项作答）

**1. 运算对象必须是整型的运算符是**

- A. /
- B. +
- C. %
- D. &lt;=

::: details 点开核对答案

**答案：C（%）** —— %（取余）要求两个操作数都是整型；/ 和 + 允许实型，关系运算符也没这个限制。

:::

**2. 设 a=10，表达式 a += a -= aa 执行后 a 的值为**

- A. 100
- B. -180
- C. -90
- D. 0

::: details 点开核对答案

**答案：B（-180）** —— 右结合：先 a -= 100 得 −90，再 a += (−90) 得 −180。2025 真题第 13 题（a=6）同型，答案 −60。

:::

**3. 表达式 3.5 + 1/2 的值是**

- A. 3.5
- B. 4.0
- C. 3.0
- D. 4.5

::: details 点开核对答案

**答案：A（3.5）** —— 1/2 两侧都是整数，整除得 0；再与 3.5 相加得 3.5。别把 1/2 当成 0.5。

:::

**4. 下列运算符中，结合方向为「从右至左」的是**

- A. +
- B. &&
- C. ,
- D. ?:

::: details 点开核对答案

**答案：D（?:）** —— 右结合只有三档：单目（2 级）、条件 ?:（13 级）、赋值（14 级）。+ 、&& 、逗号都是从左至右。

:::

**5. int i=10; 执行 int a = i++; 后 a、i 的值分别是**

- A. 11、11
- B. 10、11
- C. 10、10
- D. 11、10

::: details 点开核对答案

**答案：B（10、11）** —— 后置 i++「看整体」：a 拿到当前值 10，语句之后 i 变 11。若写 ++i 则 a、i 都是 11。

:::

**6. 设 double x=3.14; 表达式 (int)x 的值为 3，则执行后 x 的值为**

- A. 3
- B. 3.0
- C. 3.14
- D. 4.0

::: details 点开核对答案

**答案：C（3.14）** —— 强制转换不改变变量原值：(int)x 只是这个整体的值取 3，x 本身仍是 3.14。

:::

::: warning 动手环节

课后任务（约 20 分钟）

① 打开 题库/数据存储与运算-章节练习，遮住答案做其中的表达式求值与合法标识符题，错题标 ⭐️。② 回到笔记 C语言/02 §7，独立复推 `a += a -= a*a`（a=10）和 `n += n -= n*n`（n=2）两道必考题。③ 完成笔记末尾自测问题 Q10–Q13，口头作答。

:::

## 参考资料

首选精读：C语言/02-数据存储与运算 §7（15 级优先级全表与经典题组）。语法速查：[C 语法速查表](/blog/lessons/zsb-cs/reference/c-syntax-cheatsheet.html)；术语对照：[课程术语表](/blog/lessons/zsb-cs/reference/glossary.html)。格式化输出衔接下一课：[第 4 课 · 顺序结构与输入输出](/courses/zsb-cs/l/4/)。

::: info 老师的话

有哪里卡住了？直接把题目或疑问发给我——我是你的课程老师，可以逐条讲清楚，也可以针对你错的题再出几道同类题。

:::

[← 上一课：数据的表示](/courses/zsb-cs/l/2/)
[下一课：顺序结构与输入输出 →](/courses/zsb-cs/l/4/)

---

[← 第 2 课](/courses/zsb-cs/l/2/) · [课程目录](/courses/zsb-cs/) · [第 4 课 →](/courses/zsb-cs/l/4/)

