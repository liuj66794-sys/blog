---
title: 第 6 课 · 进程与子进程：Agent 跑命令的能力
createTime: 2026-08-06 11:05:10
permalink: /courses/pi-agent/l/6/
---

> 本页为摘要卡（供搜索与速览）。完整交互讲义（含随堂测）：[**打开讲义**](/blog/lessons/pi-agent/lessons/0006-nodejs-child-process.html){target="_blank"}

第 6 课 · 进程与子进程：Agent 跑命令的能力 阶段 0 · 模块 B（Node.js 核心）· 第 6 课 ｜ 预计 40 分钟 ｜ 前置：第 5 课 进程与子进程：Agent 跑命令的能力 这节课结束时，你的注册表里会多出第三个真实工具： run_command ——让程序替你跑一条终端命令（比如 npm test 、 git diff ），并把结果拿回来。这是 Coding Agent 最强大、也最危险的能力。 为什么学这个 · Mission 关联 Coding Agent 改完代码之后干什么？ 跑测试、跑构建、看 git diff ——全靠启动子进程。pi 的 bash 工具、Kimi Code 跑命令时弹出的权限确认，底层全是 Node 的 child_process 模块。PatchPilot 出口标准里的"改代码 → 跑测试 → diff"闭环，这节课补齐最后一块基石。 温故：先回忆，再学新内容 1.（第 4 课复测）不看答案： readFile 返回 Promise&lt;string&gt; ，忘了写 await 时 console.log 打出什么？ await 做了哪两件事？ 显示答案 打出 Promise { &lt;pending&gt; } ——小票本身，不是文件内容。 await 做两件事： 等 Promise 兑现 （只暂停当前函数，不冻住整个程序）＋ 把结果从小票里取出来 。 自评： 想起来了 没想起来 2.（第 1 课复测） '${name}' 、 "${name}" 、 `${name}` ——哪种引号会把 name 替换成变量的值？另外两种打出什么？ 显示答案 只有 反引号 （模板字符串）会替换。单引号和双引号都是普通字符串，会把 ${name} 四个字符原样打出来。 自评： 想起来了 没想起来 3.（第 5 课） writ…

---

[← 第 5 课 · Node.js 文件系统：Agent 的手](/courses/pi-agent/l/5/) · [课程目录](/courses/pi-agent/)
