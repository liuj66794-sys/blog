import { guide as g, frame as f, check as q } from './guide-helpers.mjs'

export const cGuides = {
  1: g('从源代码到运行结果', '能根据现象定位出错阶段，而不是把所有错误都叫作编译错误。', [
    '把程序想成一道菜：源代码是食谱，编译器把食谱翻译成机器能处理的指令，链接器把用到的各部分拼到一起，运行才是真正开始做菜。食谱写完不代表菜已经做好。',
    '以 C 程序为例，预处理处理 #include 等指令，编译与汇编产生目标文件，链接把目标文件和所需库组合成可执行文件。教学中常把预处理、编译、汇编统称为“编译阶段”；判断题要先看题目采用哪种粒度。',
    'printf 的声明让编译器知道怎样检查调用；函数实现仍需在链接时解决。语法正确只表示程序通过了相应检查，不能保证计算逻辑正确。下面按“已有文件→进行操作→得到什么”逐步看。',
  ], '同一份程序经过哪些阶段', [
    f('#include <stdio.h>\nint main(void) { printf("%d\\n", 2 + 3); return 0; }', [['当前材料', 'main.c']], '编辑阶段保存的是文本，还没有执行加法。'),
    f('预处理 → 编译 → 汇编', [['输入', 'main.c'], ['产物', '目标文件']], '检查并翻译源程序。不同工具链可能使用 .o 或 .obj 扩展名。'),
    f('目标文件 + 所需库 → 链接', [['产物', '可执行文件']], '组合代码，解析外部符号；缺少函数实现通常在这里暴露。'),
    f('启动可执行文件', [['屏幕输出', '5']], '此时程序才实际运行。若写成 2-3，仍可能成功构建，只是结果不符合原意。'),
  ], q('程序已成功生成可执行文件，但把求和写成求差。主要属于？', ['语法错误', '逻辑错误', '缺少函数实现'], 1, ['求差的语法本身合法，不能只靠语法检查发现意图错误。', '对。机器执行了写下来的运算，但它偏离了任务要求。', '缺少实现通常阻止链接成功；这里已经能运行。']),
  '某程序调用自己声明的函数 total，语法检查通过，但项目里没有它的定义。应先检查哪一步？', '先检查链接及参与链接的源文件或库。声明提供调用接口，定义提供函数体；只有声明不等于已经有可执行实现。',
  q('修复后成功编译并链接，是否证明所有输入都算对？', ['是，构建通过就是正确', '否，还要用样例和边界输入验证', '只要能输出就正确'], 1, ['构建检查不能代替需求和边界验证。', '对。还要检查预期值、空输入、边界等实际行为。', '输出存在与输出正确是两件事。'])),
  2: g('一串位怎样表示数', '能把正整数逐位换算，并在给定位宽下求负数补码。', [
    '十进制像每满十件换一箱，二进制像每满两件就向左换一箱。每一位的权重从右往左是 1、2、4、8……；位上的 1 表示取这个权重，0 表示不取。',
    '13 = 8+4+1，所以二进制是 1101。反过来，1101₂ = 1×8+1×4+0×2+1。数值不变，只是写法改变。bit 是一个二进制位；课程常见环境按 1 Byte=8 bit 计算，遇到题设应以题设为准。',
    '求负数补码必须先确定“几位”。下面限定 8 位补码：先写 +5 的 8 位形式，逐位取反再加 1 得到 -5。不要把符号位留下不变，那是把反码步骤和补码步骤混淆了。',
  ], '8 位补码中的 -5', [
    f('+5 的 8 位表示', [['位串', '00000101']], '5=4+1，左侧补 0 到 8 位。'),
    f('所有位取反', [['位串', '11111010']], '0 变 1，1 变 0，此时还没完成补码。'),
    f('最低位加 1', [['位串', '11111011']], '得到 8 位补码 -5；最高位为 1 表示该位串按有符号补码解释时为负。'),
    f('检查：00000101 + 11111011', [['低 8 位', '00000000'], ['进位', '1']], '固定 8 位运算舍去第 9 位进位，验证两者相加为 0。'),
  ], q('同一位串 11111011，解释成 8 位无符号数是多少？', ['-5', '251', '123'], 1, ['-5 是有符号补码解释；本题明确为无符号。', '对。128+64+32+16+8+2+1=251，解释规则影响数值。', '123 忽略了最高位的 128。']),
  '不用看演示，写出 6 的二进制，再求 8 位补码 -6。', '6=4+2，写成 00000110；逐位取反得 11111001；加 1 得 11111010。位宽改变时表示长度也会改变。',
  q('8 位补码 -6 的位串是哪一个？', ['10000110', '11111001', '11111010'], 2, ['这是按符号加绝对值写法得到的原码，不是补码。', '这是取反后的中间结果，还要加 1。', '对。先固定 8 位，再取反加 1。'])),
  3: g('把表达式拆成类型、运算和赋值', '能解释为什么把整数除法结果存进 double 也不能补回小数。', [
    '像把已经切掉的小数部分放进更大的盒子：盒子变大不会把丢掉的内容找回来。表达式先按操作数类型计算，之后才转换成目标变量的类型。',
    '7 和 2 都是 int，7/2 得到整数 3；赋给 double 后是 3.0。只要其中一个操作数先成为 double，例如 7.0/2，除法才得到 3.5。整数除法向零截断；余数 % 用于整数操作数。',
    '优先级决定表达式怎样分组，不等于操作数总按从左到右求值。学习时先给表达式加括号，再标各子表达式类型；不要用含有未排序的重复自增表达式来猜一个“唯一答案”。',
  ], '先计算，再放入变量', [
    f('double a = 7 / 2;', [['7 / 2', 'int: 3'], ['a', '3.0']], '除法发生在赋值之前，两边都是整数。'),
    f('double b = 7.0 / 2;', [['7.0 / 2', 'double: 3.5'], ['b', '3.5']], '2 转换后参与浮点运算。'),
    f('double c = (double)(7 / 2);', [['先除', '3'], ['c', '3.0']], '外层强制转换发生得太晚。'),
    f('double d = (double)7 / 2;', [['先转', '7.0'], ['d', '3.5']], '先改变操作数类型，再计算除法。'),
  ], q('double x = 5 / 2; 的 x 是？', ['2.5', '2.0', '无法赋值'], 1, ['先做整数除法，小数部分已截去。', '对。整数结果 2 转成 double 得到 2.0。', 'int 可以转换成 double，这不是类型错误。']),
  '把 int a=5, b=2; double avg=a/b; 改成保留小数的表达式，并说明转换应放在哪里。', '写 double avg=(double)a/b;。先把 a 转成 double，再做除法得到 2.5。写 (double)(a/b) 只能把已经得到的 2 转成 2.0。',
  q('哪种写法在除法前完成转换？', ['(double)(a/b)', '(double)a/b', 'double avg=a/b'], 1, ['括号里的整数除法已经完成。', '对。转换只作用于 a，随后参与浮点除法。', '目标变量类型不会倒过来改变右侧运算。'])),
  4: g('把输入、格式串、目标变量对齐', '能判断 scanf 成功写了几个变量，并正确检查输入失败。', [
    'scanf 像按清单收货：格式串列出预期的数据种类，地址告诉它放进哪个格子。printf 是取出值给人看，因此同一个变量在输入和输出时传入的东西可能不同。',
    'int a,b; scanf("%d %d", &a, &b) 尝试转换两个整数。&a、&b 提供可写入的地址。返回值表示成功完成的赋值数；只有返回 2 时，才能按“两个整数都读到了”继续处理。',
    '%d 等转换会跳过前导空白；%c 通常不跳过。" %c" 前面的空白指令会消耗输入空白，再读取一个字符。实际写程序还需核对类型：scanf 的 %f 对应 float*，%lf 对应 double*。',
  ], '输入 12 x 时发生了什么', [
    f('int a=0, b=0; 输入缓冲内容：12 x', [['a', '0'], ['b', '0']], '先给变量确定的初值，方便观察失败时哪些值没改。'),
    f('第一个 %d 匹配 12', [['a', '12'], ['b', '0']], '第一个转换成功，将 12 写入 a。'),
    f('第二个 %d 遇到 x', [['a', '12'], ['b', '0'], ['返回值', '1']], 'x 不能转换为整数，第二次赋值没有发生。'),
    f('if (scanf("%d %d", &a, &b) == 2) { /* 两个输入有效 */ }', [['进入成功分支', '否']], '这是本次读取的检查形式，不是再读一遍。示例返回 1，所以应进入错误处理。'),
  ], q('读取两个整数时 scanf 返回 1，说明？', ['两个变量都有效', '只成功赋值一个变量', '输入的第一个数是 1'], 1, ['返回 2 才能证明两个转换都完成。', '对。返回值统计成功赋值次数，不是输入数值。', '返回值不是输入内容本身。']),
  '输入 "7 8" 给 int a,b;，写出读取和成功时输出和的两行核心代码。', 'if (scanf("%d %d", &a, &b) == 2) { printf("%d\\n", a+b); }。输入传地址，输出传计算得到的整数值；失败分支应报告或重新处理输入。',
  q('scanf("%d", ...) 为 int n 读取数据，应该传？', ['n', '&n', '*n'], 1, ['n 是已有的整数值，不能代替目标地址。', '对。&n 指向要写入的 int 对象。', 'n 不是指针，不能在这里解引用。'])),
  5: g('条件决定哪条路径真的执行', '能在短路表达式中区分“写在右边”和“实际执行”。', [
    '门禁先验证有没有卡，再读取卡号。没有卡时，后续读取应直接跳过。这就是短路判断的直觉：左边已经足够决定结果，就不再执行右边。',
    'a && b 中，a 为 0 就跳过 b；a || b 中，a 非 0 就跳过 b。C 条件把 0 当假，把非 0 当真；关系与逻辑运算的结果为 0 或 1。',
    '读条件时画两条路径：先记左侧真假，再决定右侧是否执行，最后更新变量。尤其右侧有 ++、函数调用或除法时，不能只根据纸面出现次数来更新状态。',
  ], '右边的自增究竟有没有发生', [
    f('int x=0, y=3;', [['x', '0'], ['y', '3']], '起点。每一步只执行当前列出的语句。'),
    f('int a = x && ++y;', [['a', '0'], ['y', '3']], 'x 为假，&& 的右边不执行，y 保持 3。'),
    f('int b = x || ++y;', [['b', '1'], ['y', '4']], 'x 为假，|| 必须继续检查右侧；++y 得到 4，为真。'),
    f('if (b) x=9;', [['x', '9'], ['y', '4']], 'b 为 1，执行赋值分支。'),
  ], q('int x=1,y=2; int r=x || ++y; 最后 y 是？', ['2', '3', '1'], 0, ['对。左边已经为真，右侧自增被跳过。', '这是忽略了 || 短路。', '逻辑结果 r 为 1，但 y 没有被赋为 1。']),
  '如何写条件，保证 d 为 0 时不执行 n/d？假设 n、d 为 int 且不存在其他溢出情形。', '使用 d != 0 && n/d > 2。左侧先排除零除数；左侧为假时右侧不执行。把顺序反过来就失去了保护。',
  q('哪一项先检查除数再计算？', ['n/d>2 && d!=0', 'd!=0 && n/d>2', 'd!=0 || n/d>2'], 1, ['除法在保护条件之前，可能先触发除零。', '对。左边为假就跳过除法。', 'd 为 0 时左侧为假，|| 反而继续做除法。'])),
  6: g('switch：入口与出口分开看', '能追踪 case 命中后的穿透，而不是把每个 case 当作独立 if。', [
    '把 switch 想成从指定楼层进入楼梯：case 决定入口，break 决定出口。进入后没有出口，就会继续走到下面的语句，不会再重新匹配下一个 case。',
    'switch 的控制表达式求值后选择匹配标签；没有匹配才使用 default（如果有）。case 标签本身不阻止后续语句执行。break 跳出最内层的 switch 或循环。',
    'if/else 是另一套规则：没有花括号时，else 与最近且尚未配对的 if 结合。写嵌套条件时主动加花括号，再用缩进表达结构，不能让缩进替代语法判断。',
  ], '命中 case 1 后继续到哪里', [
    f('int k=1, s=0;', [['k', '1'], ['s', '0']], '将执行 switch(k)。'),
    f('case 1: s += 10;', [['s', '10']], '匹配入口是 case 1。这一段没有 break。'),
    f('case 2: s += 20; break;', [['s', '30']], '顺着执行 case 2 的语句，不要求 k 等于 2。随后退出 switch。'),
    f('default: s=99; /* 本例未执行 */', [['最终 s', '30']], '已经 break，不会进入 default。'),
  ], q('上例如果 k=2，s 最后是多少？', ['10', '20', '30'], 1, ['不会从 case 1 进入。', '对。直接从 case 2 开始，执行加 20 后退出。', '只有从 case 1 进入且穿透时才加两次。']),
  '同一 switch 中，让 k=1 时只加 10，应在何处补上什么？', '在 case 1 的 s+=10; 后添加 break;。这能阻止穿透到 case 2，但不会结束整个函数。',
  q('switch 放在 for 循环内，case 中的 break 退出哪一层？', ['整个函数', '最内层 switch', '外层 for'], 1, ['返回函数需要 return；break 的作用域更小。', '对。它退出最内层可被 break 终止的结构，即这里的 switch。', 'switch 更近，外层循环仍可继续。'])),
  7: g('循环每轮都要记录状态', '能按判断、循环体、更新的顺序推演，并给出退出时的变量值。', [
    '循环像每圈先验票、再跑步、最后记圈数。for 的初始化只做一次；随后反复执行“条件→循环体→更新→条件”。把更新误放到循环体前面，是很多差一错误的来源。',
    '下面用 sum 累加 1 到 3。sum=0 是起点；每轮先确认 i<=3，才把本轮 i 加进去。最后一次加完后还要执行 i++，再判断失败，所以退出时 i 是 4。',
    'continue 在 for 中会先去做更新表达式，再检查条件；break 直接离开循环，不做本轮后续更新。while 中的 continue 直接回条件，因此把更新写在它后面可能永远执行不到。',
  ], '手动走完三轮累加', [
    f('int i, sum=0; for(i=1; i<=3; i++)', [['i', '1'], ['sum', '0']], '初始化一次，准备判断 1<=3。'),
    f('sum += i; /* 第 1 轮 */', [['i', '1'], ['sum', '1']], '把 1 加入，再更新 i 到 2。'),
    f('sum += i; /* 第 2 轮 */', [['i', '2'], ['sum', '3']], '条件通过，把 2 加入，再更新到 3。'),
    f('sum += i; /* 第 3 轮 */', [['i', '3'], ['sum', '6']], '把 3 加入；这一轮完成后还要做 i++。'),
    f('i++ → 判断 4<=3 为假', [['i', '4'], ['sum', '6']], '退出时不再执行循环体，sum 不会加上 4。'),
  ], q('循环结束时 i 为什么不是 3？', ['最后还执行了 i++，然后条件失败', '因为 sum 等于 6', 'for 会自动把 i 设成 4'], 0, ['对。变量值来自实际执行顺序。', 'sum 的值不决定 i 的更新。', 'for 没有这种特殊赋值规则，是代码里的 i++ 改变它。']),
  'int s=0; for(int i=1;i<=4;i++){ if(i==2) continue; s+=i; }。先列出真正加进 s 的数，再求 s。', 'i=1 加 1；i=2 执行 continue，跳过 s+=i 但仍进行 i++；i=3 加 3；i=4 加 4。最终 s=8。',
  q('上题的 s 是？', ['10', '8', '1'], 1, ['10 是把被 continue 跳过的 2 也加了。', '对。加的是 1、3、4。', 'continue 不会终止整个循环，后面的轮次仍执行。'])),
  8: g('字符数组里还要留一个结束标志', '能区分数组容量、字符串长度和末尾空字符。', [
    '数组像一排编号储物格，下标从 0 开始；长度为 4 的数组只有 0、1、2、3 四个合法下标。声明写 4 是格子总数，不是最后一个格子的编号。',
    'C 字符串使用末尾的空字符 \\0 告诉函数“文字到这里结束”。它占一个 char 格子，但不计入 strlen 返回的字符串长度。空字符不是字符 \'0\'，也不是空格。',
    'char s[4]="cat" 恰好装下 c、a、t、\\0。若把末尾标记覆盖又没有新的标记，strlen、%s 等会继续寻找结束位置，可能越界。下面所有操作都保持有效结束标记。',
  ], '从四个格子读懂 cat', [
    f('char s[4]="cat";', [['s[0]', 'c'], ['s[1]', 'a'], ['s[2]', 't'], ['s[3]', '\\0']], '容量 4，字符串长度 3。'),
    f('s[1]=\'u\';', [['s[0]', 'c'], ['s[1]', 'u'], ['s[2]', 't'], ['s[3]', '\\0']], '只改一个元素，现在字符串是 cut，长度仍为 3。'),
    f('s[2]=\'\\0\';', [['s[0]', 'c'], ['s[1]', 'u'], ['s[2]', '\\0'], ['s[3]', '\\0']], '字符串在下标 2 结束，长度变成 2；数组容量仍为 4。'),
  ], q('最后 sizeof s 与 strlen(s) 分别是？', ['4 和 2', '2 和 2', '4 和 4'], 0, ['对。此处 s 是当前作用域中的 char 数组；容量不随内容缩短。', '修改结束标记不会缩小数组对象。', 'strlen 数到首个空字符之前，不统计全部容量。']),
  '要保存字符串 "hello"，不考虑额外输入空间，最少需要多少个 char？写出初始化。', '至少 6 个：char text[6]="hello";。5 个可见字符加 1 个空字符。也可让编译器推导：char text[]="hello";。',
  q('char text[]="hello"; 的最后一个元素是什么？', ['o', '\\0', '字符 0'], 1, ['o 是最后一个可见字符，它后面还有结束标记。', '对。字符串字面量初始化带有末尾空字符。', '字符 \'0\' 的编码不是空字符。'])),
  9: g('函数收到的是参数的副本', '能区分形参变化和调用者变量变化，并追踪返回值。', [
    '把实参传给函数，像把数字抄到另一张纸上。函数改自己的纸，不会自动改你手中的原稿。C 的参数按值传递；指针参数传递的也是地址值的副本。',
    'int add1(int x){ x=x+1; return x; } 中，x 是函数的形参。调用 add1(a) 时先取得 a 的值，初始化 x；return 把结果交回调用位置。',
    '调用者可以保存返回值，也可以忽略它。a=add1(a) 能改变 a，是调用者明确执行了赋值，不是形参 x 与 a 变成了同一个变量。',
  ], '一次调用的来回', [
    f('int a=5; int b=add1(a);', [['调用者 a', '5']], '先计算实参 a 的值 5，再进入函数。'),
    f('进入 add1：形参 int x 接收 5', [['a', '5'], ['x', '5']], '同值的两个对象，不是同一个变量。'),
    f('x=x+1;', [['a', '5'], ['x', '6']], '赋值左侧是函数自己的 x。'),
    f('return x; /* 回到调用者，初始化 b */', [['a', '5'], ['b', '6']], '返回值为 6，调用者把它保存到 b。'),
  ], q('执行 add1(a); 且不保存返回值，a 会怎样？', ['加 1', '保持不变', '被销毁'], 1, ['只改了形参副本，并没有给 a 赋值。', '对。返回值被忽略，a 未发生写入。', '函数形参的生命周期不会销毁调用者变量。']),
  '已有 int twice(int x){ return x*2; }，int n=4; n=twice(n);。写出形参初值、返回值、最后 n 的值。', '形参 x=4；返回 8；调用者把 8 赋给 n，因此最后 n=8。要指出具体改变 n 的是调用后的赋值。',
  q('哪一句负责把返回结果写回 n？', ['return x*2 中自动改 n', 'n=twice(n) 的赋值', '声明 int x'], 1, ['return 交出值，不知道调用者会存进哪个变量。', '对。赋值左侧明确指定了 n。', '形参声明只创建并初始化函数中的参数。'])),
  10: g('递归先下去，再带着结果回来', '能为每层调用保留自己的参数，并按返回顺序计算。', [
    '递归像打开嵌套盒子：每打开一层，先记住本层还欠什么工作，最里层得到答案后再逐层收尾。不能只画调用向下的箭头，返回同样属于执行过程。',
    '用阶乘作小例子：fact(n) 在 n<=1 时返回 1，否则返回 n*fact(n-1)。这里限定非负小整数，避免超出整数可表示范围。每次调用都有自己的 n。',
    '终止条件让递归停止；递归调用要朝终止条件前进。static 局部变量则跨调用保留同一份对象，不能按普通局部变量每次新建副本来推演。原课后续小节会分别展开数组参数、存储期与递归。',
  ], 'fact(3) 的调用与返回', [
    f('fact(3) = 3 * fact(2)', [['待完成', '3 × ?']], '当前层的 n=3，要先等待 fact(2)。'),
    f('fact(2) = 2 * fact(1)', [['外层等待', '3 × ?'], ['本层等待', '2 × ?']], '新调用的 n=2，外层的 n 不会被覆盖。'),
    f('fact(1) 返回 1', [['返回结果', '1']], '命中终止条件，不再递归。'),
    f('fact(2) 返回 2*1', [['返回结果', '2']], '回到 n=2 的那层，完成乘法。'),
    f('fact(3) 返回 3*2', [['最终结果', '6']], '最后才完成最外层的乘法。'),
  ], q('fact(1) 返回后，下一步先算哪一层？', ['fact(3)', 'fact(2)', '重新进入 fact(1)'], 1, ['要先完成最近等待的调用，才能得到外层的操作数。', '对。调用按栈的后进先出次序返回。', '终止条件已经返回，没有再次调用的理由。']),
  '若定义 int tick(void){ static int n=0; return ++n; }，独立程序连续调用三次会分别返回什么？', '依次返回 1、2、3。static 局部对象只初始化一次，并在调用之间保留值；名字的作用域仍在函数内。若没有 static，每次从 n=0 开始，三次都返回 1。',
  q('static 局部变量意味着什么？', ['每次调用都重新初始化', '对象跨调用保留值', '任何函数都能直接按名字访问'], 1, ['这不是 static 局部对象的初始化规则。', '对。存储期与名字作用域要分开理解。', 'static 没有把局部名字变成全局可见。'])),
  12: g('二维数组的指针每次跨过一行', '能区分元素地址、行地址及数组指针与指针数组。', [
    '二维数组像电影院按行连续排座：同一行的座位连续，不同行也按数组顺序连续存放。a+1 跨一整行，a[0]+1 只跨一个元素；它们的类型决定步长。',
    'int a[2][3]={{1,2,3},{4,5,6}}; 中，a 在多数表达式中转换为指向首行的指针，类型为 int (*)[3]。a[1] 是第二行，在多数表达式中再转换为指向该行首元素的 int*。',
    '读声明从名字附近开始：int (*p)[3] 中括号先把 *p 结合，p 是指向含 3 个 int 的数组的指针；int *p[3] 先与 [] 结合，p 是包含 3 个 int* 的数组。两种类型不能随意互换。',
  ], '从行走到具体元素', [
    f('int (*p)[3] = a;', [['第 0 行', '1 2 3'], ['第 1 行', '4 5 6']], 'p 指向首行，每步跨过 3 个 int。', 'p → 第 0 行'),
    f('p++;', [['p[0][0]', '4'], ['p[0][2]', '6']], 'p 现在指向原来的第 1 行；不能再向后走后解引用。', 'p → 第 1 行'),
    f('int x = *(*(p)+1);', [['*p', '第 1 行'], ['x', '5']], '先定位这一行，再走一个元素，最后取值。', '目标 → a[1][1]'),
  ], q('原数组 a 的 a+1 相比 a 跨过多少个 int？', ['1', '2', '3'], 2, ['那是元素指针的一步，不是行指针的一步。', '2 是行数，不是每行元素数。', '对。一行有 3 个 int，步长由所指行类型决定。']),
  'int b[2][4]; 要声明指向 b 首行的指针 r，写出类型和初始化。', 'int (*r)[4]=b;。r 指向包含 4 个 int 的数组。r+1 跨 4 个 int 的大小，而不是固定 4 字节。',
  q('哪个声明符合题意？', ['int *r[4]=b;', 'int (*r)[4]=b;', 'int **r=b;'], 1, ['这是指针数组，不是行指针。', '对。括号保证 r 是指针，所指对象是一行。', '二维连续数组并不是 int**，不能用它替代行指针。'])),
  13: g('先看对象，再看访问路径', '能通过对象或指针修改同一个结构体，并用成功分支管理文件。', [
    '结构体像一张学生档案，把姓名、分数等不同字段归在同一个对象里。对象名.成员访问该对象的字段；指针->成员先找到对象再访问字段。',
    'struct Student { int id; int score; }; 定义类型形状；struct Student s={7,80}; 才创建对象。若 p=&s，则 p->score 与 (*p).score 访问同一个成员，括号不能省略。',
    '文件也要先得到有效访问入口。FILE *fp=fopen("data.txt","r"); 后先判断 fp 是否为空；仅在成功时读取，核对读取函数结果，并在使用结束后 fclose(fp)。打开失败后不能继续把空指针传给读取函数。',
  ], '同一份档案的两种访问方式', [
    f('struct Student s={7,80};', [['s.id', '7'], ['s.score', '80']], '按成员声明顺序初始化。'),
    f('struct Student *p=&s;', [['s.score', '80'], ['p', '&s']], '创建访问 s 的指针，档案没有复制。', 'p → s'),
    f('p->score=90;', [['s.score', '90'], ['(*p).score', '90']], '两种写法到达同一个字段。', 'p → s'),
    f('struct Student copy=s; copy.score=60;', [['s.score', '90'], ['copy.score', '60']], '结构体赋值产生另一个对象的成员值；改 copy 的整数字段不改 s。'),
  ], q('p 指向 s，修改 p->score 会怎样？', ['只改 p 中的地址', '改 s.score', '生成新的 Student'], 1, ['赋值左侧是成员访问，不是 p 本身。', '对。沿 p 找到 s，再修改其 score。', '成员写入不会额外创建结构体对象。']),
  'fopen 返回空指针时，下一步应继续 fscanf 还是进入失败处理？说明理由。', '进入失败处理。空指针表示未取得有效文件流，不能继续读写或调用 fclose(NULL)。成功打开后，也要检查读取是否成功，最后关闭有效文件流。',
  q('哪条执行顺序合理？', ['fopen→直接读取→判断是否为空', 'fopen→检查成功→读取并检查→关闭', '读取→fopen→关闭'], 1, ['判断太晚，读取时就可能已经使用无效流。', '对。先取得资源，再使用，使用后释放。', '必须先成功打开才能通过对应文件流读取。'])),
}
