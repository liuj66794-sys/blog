# 个人技术博客站点建设方案

> 版本：v1.0 ｜ 日期：2026-08-14
> 目标：把「学习内容（D:\01-Documents\learn）+ 课程页面 + GitHub 项目 + 个人主页」整合为一个静态技术博客站点，部署到 GitHub Pages。

---

## 1. 需求与现状

### 1.1 需求（四合一）

| # | 需求 | 说明 |
|---|------|------|
| 1 | 学习内容展示 | `D:\01-Documents\learn` 下 6 个子项目的讲义、笔记、参考资料 |
| 2 | 课程页面 | 支持「课程 → 模块 → 周 → 课」的多级导航与翻页 |
| 3 | GitHub 项目 | 展示 `liuj66794-sys` 主页的置顶项目、统计 |
| 4 | 个人主页 | 首页：简介、技能、社交链接、入口导航 |

### 1.2 现状盘点

**内容资产（learn 目录）：**

| 子项目 | 内容形态 | 站点定位 |
|--------|----------|----------|
| `gupiao` | 22 周 A 股课程，`lessons/*.html` 讲义 + 《课程总纲.md》+ 模块/周/课三级结构 | 主力课程 |
| `pi_agent_learning` | TypeScript Agent 课程，HTML 讲义 + design.md + 学习计划 | 课程 |
| `mattpocock-skills-learning` | 技能课程（grill/to-spec/to-tickets 等），HTML 讲义 + specs/docs | 课程 |
| `English` | 英语学习/教学 | 课程/知识库 |
| `policy` | 政策学习 | 课程/知识库 |
| `ts-playground` | TS 练习 | 课程/练习页 |
| 各项目 `learning-records/` | Markdown 学习记录 | 博客文章素材 |
| 各项目 `reference/`、`docs/` | Markdown 参考资料 | 知识库 |

**关键事实：讲义是自带 CSS 的独立 HTML**（如 `gupiao/lessons/0001-stock-is-ownership.html` 引用 `assets/lesson.css`，含随堂测交互），样式自洽、相对路径引用资源。

**已有资产（GitHub）：**
- 账号 `liuj66794-sys`，9 个公开仓库：PolicyAnalyzerPro、Tlisily、boxuegu、mattpocock-skills-learning、glm-5.3-intro 等
- 已有 cyberpunk 风格主页 README 仓库（`liuj66794-sys/liuj66794-sys`）
- `gh` CLI 已登录，可拉取仓库数据

**站点目录**：`D:\00-Projects\test\windsuft\glm\blog`（当前为空）。

---

## 2. 方案选型

### 2.1 结论：VuePress 2 + Plume 主题

选型对比（Star 数据截至 2026-08-14，GitHub API 实测）：

| 方案 | Stars | 匹配度 | 理由 |
|------|-------|--------|------|
| **VuePress 2 + Plume** ✅ | 主题 4.8k | ★★★★★ | 唯一原生支持「博客 + 文档/专栏 + 知识库」一体的主题；中文文档；开箱即用 |
| Astro + Fuwari | 61.8k / 4.9k | ★★★★☆ | 更现代，但课程多级导航需自建 |
| Hexo + NexT | 41.8k / 2.8k | ★★★☆☆ | 经典中文博客，但课程/作品集支持弱 |
| Hugo / Docusaurus / VitePress | 89k / 66k / 18k | 视场景 | 纯文档或纯博客，需大量二次开发 |

**Plume 胜出的核心能力**（对应需求 1、2）：
- **集合（Collection）机制**：`post` 集合 = 博客（文章列表/分类/标签/归档）；`doc` 集合 = 文档/教程/知识库（自动侧边栏、多级嵌套、目录）
- **教程专栏开箱即用**：官方定位就是「博客、专栏、系列教程、知识库、产品文档」
- **中文生态**：文档、社区、示例站均为中文，学习成本低

### 2.2 GitHub 项目展示选型

| 方式 | 说明 | 适用 |
|------|------|------|
| **github-readme-stats 置顶卡片**（首选） | `https://github-readme-stats.vercel.app/api/pin/?username=liuj66794-sys&repo=xxx` 一行 Markdown 嵌入，79.8k stars 长期维护 | 展示置顶 6 仓，零开发 |
| 构建时 GitHub API 抓取（增强） | CI 里用 `gh api` / token 拉仓库列表生成 `projects.md`，可含私有仓与描述 | 想要完整项目墙 + 可控性 |
| al-folio 式 `repositories.yml` | 声明式配置仓库清单自动渲染 | 配置化但引入 Jekyll 生态，不采用 |

推荐：**先置顶卡片，后加 API 生成的项目页**（两个都保留，互不冲突）。

---

## 3. 站点信息架构

```
liuj66794-sys.github.io
├── 首页（/）          个人主页：头像、简介、技能、四入口导航、GitHub 统计卡
├── 博客（/blog/）     学习笔记（post 集合）｜ 分类、标签、归档、全文搜索
├── 课程（/courses/）  6 门课程（doc 集合）｜ 模块→周→课多级侧边栏 + 翻页
├── 知识库（/knowledge/） 参考资料（doc 集合）｜ 长期沉淀的词典/总纲/ADR
└── 项目（/projects/） GitHub 项目墙：置顶卡片 + 全仓库列表
```

页面语言：中文。风格：延续 GitHub 主页的 cyberpunk 视觉基因（可选，见 §8 风险项）。

---

## 4. 内容迁移方案（learn → 站点）

### 4.1 核心决策：讲义 HTML 怎么进站

| 方案 | 做法 | 优点 | 缺点 |
|------|------|------|------|
| **A. 原样托管（过渡期采用）** | 讲义 HTML + `assets/` 整目录复制到 `docs/public/lessons/`；课程目录页（Markdown）链接过去 | 零改造、即时上线、保留随堂测交互 | 页面无主题外观、无搜索索引、无自动目录 |
| B. 转 Markdown（长期演进） | 讲义改为输出 `.md` 纳入 doc 集合 | 统一主题、全文搜索、TOC、深色模式 | 需开发 HTML→MD 转换/生成端改造 |
| C. 混合（推荐落地路径） | 先 A 全量上线；选 gupiao 一门课试点转 MD（方案 B），验证体验后逐步铺开 | 渐进式、风险低 | 双轨期间需维护两套讲义 |

**推荐：C（先 A 后 B 渐进）**。理由：内容已在生成中（AI 滚动生成讲义），生成端改为输出 Markdown 是自然演进，不阻塞上线。

### 4.2 课程集合目录设计（doc 集合，自动侧边栏）

以 gupiao 为例（映射《22 周课程总纲》的模块/周结构）：

```
docs/courses/a-shares/
├── README.md              # 课程首页：总纲、入口、进度
├── module-0/科二冲刺周.md
├── module-1/概念扫盲/
│   ├── README.md
│   ├── 01-股票与股东.md
│   ├── 02-交易所与板块.md
│   └── …
└── module-2/…
```

Plume 对 doc 集合自动「基于文件结构生成侧边栏」，多级嵌套天然支持，无需手工维护导航。

### 4.3 同步脚本

`scripts/sync-learn.mjs`（Node，无框架依赖）：
1. 遍历 `D:\01-Documents\learn/*` 六个子项目
2. 复制 `lessons/*.html` + `assets/` → `docs/public/lessons/<project>/`
3. 复制/转换 `learning-records/*.md` → `docs/blog/<project>/`（补 frontmatter：title/createTime/tags）
4. 复制 `reference/`、`docs/`、课程总纲 → `docs/knowledge/<project>/`
5. 生成课程目录 README 骨架

增量同步：比对 mtime 跳过未变更文件；可挂 npm script `npm run sync` 手动触发。

---

## 5. GitHub 项目页设计

### 5.1 置顶卡片（Markdown 一行嵌入）

```markdown
[![PolicyAnalyzerPro](https://github-readme-stats.vercel.app/api/pin/?username=liuj66794-sys&repo=PolicyAnalyzerPro&theme=tokyonight&locale=cn)](https://github.com/liuj66794-sys/PolicyAnalyzerPro)
```

参数：`theme`（tokyonight 等暗色系，贴合 cyberpunk 风格）、`locale=cn`、`hide_border`。

### 5.2 项目墙（构建时生成）

`scripts/fetch-projects.mjs`：调 GitHub API（CI 内用 `GH_TOKEN`）拉取 `liuj66794-sys` 全部公开仓库（名称、描述、语言、stars、主页地址），渲染 `docs/projects/index.md`。展示为卡片网格，突出 4 个主力项目：
- **PolicyAnalyzerPro**（政策文本分析桌面软件）
- **Tlisily**（AI 角色扮演桌面应用）
- **boxuegu**（uni-app 在线学习平台）
- **mattpocock-skills-learning**（技能学习仓库，与站点课程联动）

---

## 6. 个人主页设计

`docs/home.md`（Plume 自定义首页布局）+ 顶部导航：
- 头像、一句话简介（沿用 GitHub 主页的 L1U.J / 0x6C6975 人设）
- 技能标签（TypeScript、Vue、Python、NLP、Agent 应用）
- 四入口卡片：博客 / 课程 / 知识库 / 项目
- GitHub 统计卡：`github-readme-stats` 的 stats 卡 + 常用语言卡
- 社交链接：GitHub、邮箱

---

## 7. 技术方案与部署

### 7.1 技术栈

| 项 | 选型 |
|----|------|
| 框架 | VuePress 2（Vue 3 + Vite） |
| 主题 | `vuepress-theme-plume`（v2.x，活跃维护） |
| 脚手架 | `npm create vuepress-theme-plume@latest` |
| 配置 | `docs/.vuepress/config.ts`（`plumeTheme({...})`） |
| 内容 | Markdown（含 HTML 静态资源挂载） |
| 包管理 | pnpm |
| 部署 | GitHub Actions → GitHub Pages（静态产物，可部署到任意静态托管） |

### 7.2 关键配置示意

```ts
// docs/.vuepress/config.ts
plumeTheme({
  collections: {
    posts: { dir: 'blog', title: '博客' },        // post 集合
    docs: [                                        // doc 集合
      { dir: 'courses/a-shares',    title: 'A 股入门' },
      { dir: 'courses/pi-agent',    title: 'Agent 学习' },
      { dir: 'courses/mattpocock',  title: '工程技能' },
      { dir: 'knowledge',           title: '知识库' },
    ],
  },
  plugins: {
    search: { locales: { '/': { placeholder: '搜索' } } }, // 全文搜索
  },
})
```

### 7.3 GitHub Pages 部署

仓库：直接用 `liuj66794-sys/liuj66794-sys.github.io`（或先在 `blog` 仓库启用 Pages 后迁移）。`.github/workflows/deploy.yml` 工作流：

```yaml
on:
  push: { branches: [main] }
jobs:
  build-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - run: pnpm install && pnpm docs:build   # 构建静态站点
      - run: node scripts/fetch-projects.mjs   # 生成项目墙（用 GH_TOKEN）
      - uses: peaceiris/actions-gh-pages@v4    # 发布到 gh-pages 分支
        with: { github_token: ${{ secrets.GITHUB_TOKEN }}, publish_dir: docs/.vuepress/dist }
```

> 注意：项目墙在构建后生成，需在 `docs:build` 之后再跑并重新 build 一次，或将抓取结果写为构建期插件（实施时二选一，推荐插件方式）。

---

## 8. 风险与备选

| 风险 | 影响 | 对策 |
|------|------|------|
| Plume 相对小众（4.8k stars） | 长期维护风险 | 主题作者 2 天前仍在更新；内容全为 Markdown 可迁移，锁定期成本低 |
| 讲义转 MD 成本 | 阶段 2 延期 | 阶段 1 全量走方案 A（原样托管），转 MD 仅试点 |
| github-readme-stats 依赖第三方 Vercel | 卡片偶发不可用 | 备选自托管该服务；或直接走 GitHub API 生成项目墙 |
| cyberpunk 视觉风格迁移 | 首页主题不统一 | 先做内容后做视觉，Plume 主题变量可定制，风格作为迭代项 |
| Node 版本要求（≥20.19） | 本地环境兼容 | 开发机安装 Node LTS，CI 固定版本 |

---

## 9. 实施步骤（里程碑）

| 阶段 | 内容 | 工时 | 验收标准 |
|------|------|------|----------|
| **M0 骨架** | 脚手架初始化、配置、GitHub Actions 部署打通 | 0.5 天 | 空站 `liuj66794-sys.github.io` 可访问 |
| **M1 主页+项目** | 首页（个人主页）+ 置顶卡片 + 项目墙 + 统计卡 | 1 天 | 四入口导航可点、GitHub 项目可见 |
| **M2 课程上线** | `sync-learn.mjs` + 6 门课程目录 + 讲义 HTML 托管 | 2-3 天 | 6 门课可浏览、侧边栏导航正确、同步脚本幂等 |
| **M3 知识库+博客** | learning-records 转 post 集合、reference 进知识库、全文搜索 | 1 天 | 博客分类/标签/搜索可用 |
| **M4 体验优化** | gupiao 讲义转 MD 试点、深色模式、评论、SEO、域名（可选） | 按需 | 讲义统一主题外观 |

合计：**约 5 个工作日完成 MVP（M0–M3）**。

---

## 10. 附录：参考资源

- Plume 主题官方文档：https://theme-plume.vuejs.press/
- Plume 集合机制指南：https://theme-plume.vuejs.press/guide/collection/
- github-readme-stats（置顶卡片 API）：https://github.com/anuraghazra/github-readme-stats
- GitHub Pages 部署：https://docs.github.com/pages
